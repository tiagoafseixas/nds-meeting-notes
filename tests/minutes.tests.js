var MinutesController = require('../app/controllers/Minutes');
var Immutable = require('immutable');

test(
    'Test Add Minute With Error!',
    () => {
        let mockMinute = Immutable.Map({

        });
    }
);